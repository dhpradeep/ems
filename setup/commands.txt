1)sudo docker-compose up --build -d

2) bash commands.sh

OR

2) source development.env
3) sudo docker exec -i $(sudo docker inspect --format="{{.Id}}" ems_db) mysql -h$MYSQL_HOST -u$MYSQL_USER -p$MYSQL_PASSWORD $MYSQL_DATABASE < ems.sql

OR

2) Get the variable values from development.env and

sudo docker exec -i $(sudo docker inspect --format="{{.Id}}" ems_db) mysql -h$<hostname> -u<username> -p<password> <db_name> < ems.sql


